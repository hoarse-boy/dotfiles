###################
### KEYBINDINGS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/
bind = $mainMod, A, exec, swaync-client -t -sw
# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
bind = $mainMod, grave, exec, ~/.config/rofi/scripts/key.sh

# bind = $mainMod, R, exec, $menu || pkill rofi # FIX: . check and test this. remove comments later:

# bind = $mainMod, B, exec, brave # DEL: . DELETE LINES LATER
# bind = $mainMod, Backspace, exec, ~/.config/hypr/scripts/Wlogout.sh # FIX: . make it not spawn on each click. change the style
bind = $mainMod, Backspace, exec, ~/.config/wlogout/launch-wlogout.sh # FIX: . change loc?
# bind = $mainMod Alt, P, exec, ~/.config/hypr/scripts/Wlogout.sh # DEL: . DELETE LINES LATER

# bind = $mainMod, W, exec, brave --new-window https://web.whatsapp.com # DEL: . DELETE LINES LATER

# Toggle overview (Mission Control style)

bind = Super, Tab, global, quickshell:overviewToggle
bind = Super Shift, B, exec, ~/.config/waybar/scripts/waybar-theme-rofi.sh

# Stash operations
# bind = Super Shift, V, global, quickshell:stashWorkspace # DEL: . DELETE LINES LATER
# bind = Super Shift, U, global, quickshell:unstashQuick

bind = Super+Shift, S, global, quickshell:regionScreenshot


# bind = $mainMod Alt, M, exec, qs -c ~/.config/quickshell/music msg music toggle # FIX: . change

# Image search (Google Lens)
bind = Super+Shift, A, global, quickshell:regionSearch

bind = Super Alt, R, exec, ~/.config/rofi/scripts/rofi-theme-switcher.sh # FIX: . not working
 # FIX: . make sure the waybar config.jsonc is ignored in git. it will be broken once it is pushed
# and make a script instead to generate it. which should be different than rofi-theme-switcher
# it will be for installation only

# OCR text extraction
bind = Super+Shift, X, global, quickshell:regionOcr # FIX: . i think this is nice. need test

# Region recording
bind = Super+Shift, R, global, quickshell:regionRecord # FIX: . check and test this. remove comments later:

# Region recording with audio
bind = Super+Shift+Alt, R, global, quickshell:regionRecordWithSound # FIX: . check and test this. remove comments later:

# Color picker (requires hyprpicker)
bind = Super+Shift, C, exec, hyprpicker -a

 # FIX: . make it using var like HYPRSCRIPTS etc
bind = $mainMod Alt, W, exec, ~/.config/hypr/wppicker.sh # FIX: . check and test this. remove comments later: wallpaper hyprkenso here
bind = $mainMod Alt, T, exec, ~/.config/hypr/themepicker.sh
bind = $mainMod Alt, S, exec, ~/.config/hypr/syslauncher.sh
# bind = $mainMod, Q, killactive, # DEL: . DELETE LINES LATER
# bind = $mainMod Alt, Q, exit, # DEL: . DELETE LINES LATER
bind = $mainMod Alt, B, exec, ~/.config/waybar/scripts/launch.sh
bind = $mainMod Alt, H, exec, ~/.config/waybar/scripts/hide.sh

bind = $mainMod, E, exec, $fileManager # FIX: . check and test this. remove comments later:

# bind = $mainMod, space, togglefloating, active # DEL: . DELETE LINES LATER
# bind = $mainMod, F, fullscreen
# bind = $mainMod, J, togglesplit, # dwindle # DEL: . DELETE LINES LATER

# # Move focus with mainMod + arrow keys # DEL: . DELETE LINES LATER
# bind = $mainMod, left, movefocus, l
# bind = $mainMod, right, movefocus, r
# bind = $mainMod, up, movefocus, u
# bind = $mainMod, down, movefocus, d

# # Switch workspaces with mainMod + [0-9] # DEL: . DELETE LINES LATER
# bind = $mainMod, 1, workspace, 1
# bind = $mainMod, 2, workspace, 2
# bind = $mainMod, 3, workspace, 3
# bind = $mainMod, 4, workspace, 4
# bind = $mainMod, 5, workspace, 5
# bind = $mainMod, 6, workspace, 6
# bind = $mainMod, 7, workspace, 7
# bind = $mainMod, 8, workspace, 8
# bind = $mainMod, 9, workspace, 9
# bind = $mainMod, 0, workspace, 10

# # Move active window to a workspace with mainMod + SHIFT + [0-9]
# bind = $mainMod SHIFT, 1, movetoworkspace, 1
# bind = $mainMod SHIFT, 2, movetoworkspace, 2
# bind = $mainMod SHIFT, 3, movetoworkspace, 3
# bind = $mainMod SHIFT, 4, movetoworkspace, 4
# bind = $mainMod SHIFT, 5, movetoworkspace, 5
# bind = $mainMod SHIFT, 6, movetoworkspace, 6
# bind = $mainMod SHIFT, 7, movetoworkspace, 7
# bind = $mainMod SHIFT, 8, movetoworkspace, 8
# bind = $mainMod SHIFT, 9, movetoworkspace, 9
# bind = $mainMod SHIFT, 0, movetoworkspace, 10
# bindd = ALT, tab, cycle next window, cyclenext
# bindd = ALT, tab, bring active to top, bringactivetotop # DEL: . DELETE LINES LATER

# # Resize windows # DEL: . DELETE LINES LATER
# binded = $mainMod SHIFT, left, resize left (-50), resizeactive, -50 0
# binded = $mainMod SHIFT, right, resize right (+50), resizeactive, 50 0
# binded = $mainMod SHIFT, up, resize up (-50), resizeactive, 0 -50
# binded = $mainMod SHIFT, down, resize down (+50), resizeactive, 0 50

# # Move windows # DEL: . DELETE LINES LATER
# bindd = $mainMod CTRL, left, move window left, movewindow, l
# bindd = $mainMod CTRL, right, move window right, movewindow, r
# bindd = $mainMod CTRL, up, move window up, movewindow, u
# bindd = $mainMod CTRL, down, move window down, movewindow, d

# # Swap windows
# bindd = $mainMod ALT, left, swap window left, swapwindow, l
# bindd = $mainMod ALT, right, swap window right, swapwindow, r
# bindd = $mainMod ALT, up, swap window up, swapwindow, u
# bindd = $mainMod ALT, down, swap window down, swapwindow, d # DEL: . DELETE LINES LATER


 # FIX: . what is this?
# Move active window to a workspace silently mainMod + CTRL [0-9] # FIX: . check and test this. remove comments later:
# bindd = $mainMod CTRL, code:10, move silently to workspace 1, movetoworkspacesilent, 1 # NOTE: code:10 = key 1
# bindd = $mainMod CTRL, code:11, move silently to workspace 2, movetoworkspacesilent, 2 # NOTE: code:11 = key 2
# bindd = $mainMod CTRL, code:12, move silently to workspace 3, movetoworkspacesilent, 3 # NOTE: code:12 = key 3
# bindd = $mainMod CTRL, code:13, move silently to workspace 4, movetoworkspacesilent, 4 # NOTE: code:13 = key 4
# bindd = $mainMod CTRL, code:14, move silently to workspace 5, movetoworkspacesilent, 5 # NOTE: code:14 = key 5
# bindd = $mainMod CTRL, code:15, move silently to workspace 6, movetoworkspacesilent, 6 # NOTE: code:15 = key 6
# bindd = $mainMod CTRL, code:16, move silently to workspace 7, movetoworkspacesilent, 7 # NOTE: code:16 = key 7
# bindd = $mainMod CTRL, code:17, move silently to workspace 8, movetoworkspacesilent, 8 # NOTE: code:17 = key 8
# bindd = $mainMod CTRL, code:18, move silently to workspace 9, movetoworkspacesilent, 9 # NOTE: code:18 = key 9
# bindd = $mainMod CTRL, code:19, move silently to workspace 10, movetoworkspacesilent, 10 # NOTE: code:19 = key 0
# bindd = $mainMod CTRL, bracketleft, move silently to previous workspace, movetoworkspacesilent, -1 # brackets [
# bindd = $mainMod CTRL, bracketright, move silently to next workspace, movetoworkspacesilent, +1 # brackets ]

# bind = $mainMod, t, exec, hyprctl dispatch setprop active opaque toggle # FIX: . ?

# Scroll through existing workspaces with mainMod + scroll # FIX: . good
bindd = $mainMod, mouse_down, next workspace, workspace, e+1
bindd = $mainMod, mouse_up, previous workspace, workspace, e-1
# bindd = $mainMod, period, next workspace, workspace, e+1 # FIX: . check and test this. remove comments later:
# bindd = $mainMod, comma, previous workspace, workspace, e-1 # FIX: . check and test this. remove comments later:

# Move/resize windows with mainMod + LMB/RMB and dragging # FIX: . nice
bindmd = $mainMod, mouse:272, move window, movewindow # NOTE: mouse:272 = left click
bindmd = $mainMod, mouse:273, resize window, resizewindow # NOTE: mouse:272 = right click


 # FIX: . move to workspaces.conf


 # FIX: . move to its current dir
 # FIX: . disabled binding
 # hyprland will run binding with the same mapping so unbind if it isfrom other dotifles
 #

# █▄▀ █▀▀ █▄█ █▄▄ █ █▄░█ █▀▄ █ █▄░█ █▀▀ █▀
# █░█ ██▄ ░█░ █▄█ █ █░▀█ █▄▀ █ █░▀█ █▄█ ▄█

# ==================== KEYBINDING CONVENTIONS ====================
# SUPER (Windows key) - Main window management
# ALT - Workspace navigation and app switching
# CTRL - Reserved for in-app shortcuts if used alone. otherwise, can be used with other modifiers. ex. SUPER + CTRL + K
# SHIFT - Modifier for alternate actions

# ==================== TIPS TO ADD NEW KEYBINDING ====================
# search the existing keybdingins
# create new binding at the below VARIABLES section
# create new binding and use this to test first:
# bindd = $mainMod, X, Test binding, exec, notify-send "Test" "this is just a test to show the keybinding is working" -t 2000

# this the fastest way to restore tabs from history without spawning chrased pwa
# exec-once = notify-send "Chromium" "Ctrl-shift-t to restore tabs from history if chromium crashed" -t 2000 

 # FIX: . move this
# TODO: testing ydotool
# bind = SUPER, R, exec, ydotool mousemove -w   -- 0 -1  # scrolling down
# bind = SUPER, R, exec, ydotool mousemove -w 0 100  # also scrolling up
# bind = SUPER, C, exec, ydotool mousemove --wheel -- 0 5 # FIX: . not working
# bind = SUPER, C, exec, wlrctl pointer scroll 10 0
# bind = SUPER, C, exec, ydotool mousemove --wheel 5 10

# ==================== NOTIFICATION ====================
bindd = $mainMod ALT CTRL, R, Hide latest notification, exec, swaync-client --hide-latest
bindd = $mainMod ALT CTRL, N, Hide latest notification, exec, swaync-client -t

# ==================== APPLICATIONS ====================
bindd = $mainMod, T, Launch terminal, exec, $term
bindd = $mainMod, B, Launch browser, exec, $browser
# bindd = $mainMod, B, Launch browser, exec, ~/.config/ml4w/settings/browser.sh
# bindd = $mainMod, E, Open file manager, exec, ~/.config/ml4w/settings/filemanager.sh # FIX: . check and test this. remove comments later:
bindd = $mainMod CTRL, E, Show emoji picker, exec, ~/.config/ml4w/settings/emojipicker.sh
bindd = $mainMod CTRL, C, Open calculator, exec, ~/.config/ml4w/settings/calculator.sh
bindd = ALT, Space, Application launcher, exec, pkill rofi || rofi -show drun -replace -i # FIX: . check and test this. remove comments later:
bindd = ALT, F, Toggle Floating, togglefloating


# ==================== FLOATING APPLICATIONS ====================
bind = $mainMod, R, exec, kitty --class $fMpd rmpc # tui music player
bind = $mainMod, Z, exec, kitty --class $fTerm btop # performance monitor
bind = $mainMod, Y, exec, kitty --class $fTerm yazi # tui file manager

# ==================== WINDOW MANAGEMENT ====================
bindd = $mainMod, Q, Close window, killactive
bindd = $mainMod SHIFT, Q, Quit application, exec, hyprctl activewindow | grep pid | tr -d 'pid:' | xargs kill
bindd = $mainMod, F, Toggle fullscreen, fullscreen, 0
bindd = $mainMod, M, Maximize window, fullscreen, 1
bindd = $mainMod SHIFT, T, Float all windows, workspaceopt, allfloat
bindd = ALT, S, Toggle split, togglesplit # changing the window orientation of vertical or horizontal
# bindd = $mainMod, G, Toggle group, togglegroup # disable this as G is used
# bindd = $mainMod, K, Swap split, swapsplit
bindd = ALT, Tab, Cycle Next Window, cyclenext

# Window movement with mouse
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# ==================== SCREENSHOTS ====================
bind = , Print, exec, ~/.local/bin/screenshot-avif fullscreen
bind = $mainMod, Print, exec, ~/.local/bin/screenshot-avif window
bind = $mainMod, P, exec, ~/.local/bin/screenshot-avif region
bindd = $mainMod SHIFT, PRINT, Ml4W screenshot, exec, $HYPRSCRIPTS/screenshot.sh # Take a screenshot using ml4w script. usefull one are the delay for certain app that has the ui closed when typing a keybinding.

## png screenshot
# bindd = , Print, Copy screen to clipboard, exec, grim - | wl-copy && notify-send "Screenshot" "Full screen copied to clipboard!" -t 2000
# bindd = $mainMod, Print, Copy active window, exec, grim -g "$(hyprctl activewindow -j | jq -r '"\(.at[0]),\(.at[1]) \(.size[0])x\(.size[1])"')" - | wl-copy && notify-send "Screenshot" "Active window copied to clipboard!" -t 2000
# bindd = $mainMod, P, Copy selected area, exec, grim -g "$(slurp)" - | wl-copy && notify-send "Screenshot" "Region copied to clipboard!" -t 2000

# ==================== SYSTEM ====================
bindd = $mainMod CTRL, R, Reload Hyprland, exec, hyprctl reload
# bindd = $mainMod, BACKSPACE, Power Menu, exec, sh $HYPRSCRIPTS/power_management.sh # FIX: . check and test this. remove comments later:
# bindd = $mainMod SHIFT, W, Random wallpaper, exec, waypaper --random # FIX: . check and test this. remove comments later:
bindd = $mainMod CTRL SHIFT, W, Wallpaper selector, exec, waypaper # FIX: . check and test this. remove comments later:
# enable it after changing the sript.
# bindd = $mainMod ALT, W, Start wallpaper automation, exec, $HYPRSCRIPTS/wallpaper-automation.sh
# bindd = $mainMod ALT CTRL SHIFT, K, Show keybindings, exec, ~/.config/hypr/scripts/my-keybindings.sh # FIX: . check and test this. remove comments later:

# bindd = $mainMod SHIFT, B, Reload Waybar, exec, ~/.config/waybar/launch.sh # FIX: . change eww and find how to close it
# bindd = $mainMod CTRL, B, Toggle Waybar, exec, ~/.config/waybar/toggle.sh # FIX: . check and test this. remove comments later:

# bindd = $mainMod, V, Clipboard manager, exec, $HYPRSCRIPTS/cliphist.sh # FIX: . reboot and test again. is clipboard fine? is wl-copy starts?
bind = CTRL Alt, Delete, exec, cliphist wipe | pkill -USR1 rofi # FIX: . check and test this. remove comments later:
bind = $mainMod , V, exec, ~/.config/hypr/clip.sh # FIX: . why empty?

# bind = $mainMod, V, exec, $HYPRSCRIPTS/cliphist.sh # FIX: . reboot and test again. is clipboard fine? is wl-copy starts?
 # FIX: . find a way to overwrite illogical or just copy all to mine?

# ==================== WORKSPACES ====================
# Workspace switching
# bindd = $mainMod, 1, Workspace 1, workspace, 1 # old binding
bindd = $mainMod, 1, Workspace 1, exec, ~/.config/hypr/scripts/toggle-workspace.sh 1
bindd = $mainMod, 2, Workspace 2, exec, ~/.config/hypr/scripts/toggle-workspace.sh 2
bindd = $mainMod, 3, Workspace 3, exec, ~/.config/hypr/scripts/toggle-workspace.sh 3
bindd = $mainMod, 4, Workspace 4, exec, ~/.config/hypr/scripts/toggle-workspace.sh 4
bindd = $mainMod, 5, Workspace 5, exec, ~/.config/hypr/scripts/toggle-workspace.sh 5
bindd = $mainMod, 6, Workspace 6, exec, ~/.config/hypr/scripts/toggle-workspace.sh 6
bindd = $mainMod, 7, Workspace 7, exec, ~/.config/hypr/scripts/toggle-workspace.sh 7
bindd = $mainMod, 8, Workspace 8, exec, ~/.config/hypr/scripts/toggle-workspace.sh 8
bindd = $mainMod, 9, Workspace 9, exec, ~/.config/hypr/scripts/toggle-workspace.sh 9
bindd = $mainMod, 0, Workspace 10, exec, ~/.config/hypr/scripts/toggle-workspace.sh 10

# Workspace navigation
bindd = $mainMod ALT, Tab, Next workspace, workspace, m+1
bindd = $mainMod ALT SHIFT, Tab, Previous workspace, workspace, m-1
bindd = $mainMod, mouse_down, Next workspace, workspace, e+1

# bindd = $mainMod, $vimRight, Next workspace (vim), workspace, r+1
# bindd = $mainMod, $vimLeft, Previous workspace (vim), workspace, r-1
# bindd = $mainMod, $vimDown, Open Empty Workspace, workspace, empty
bindd = $mainMod, $vimRight, Next workspace, exec, ~/.config/hypr/scripts/toggle-workspace.sh right
bindd = $mainMod, $vimLeft, Previous workspace, exec, ~/.config/hypr/scripts/toggle-workspace.sh left
bindd = $mainMod, $vimDown, Open Empty Workspace, exec, ~/.config/hypr/scripts/toggle-workspace.sh down
bindd = $mainMod, Tab, Go back and forth to previous workspace, workspace, previous # must enable this allow_workspace_cycles

# ==================== FUNCTION KEYS ====================
bindd = , XF86MonBrightnessUp, Increase brightness, exec, brightnessctl -q s +10%
bindd = , XF86MonBrightnessDown, Decrease brightness, exec, brightnessctl -q s 10%-

bindd = , XF86AudioRaiseVolume, Increase volume, exec, swayosd-client --output-volume raise
bindd = , XF86AudioLowerVolume, Decrease volume, exec, swayosd-client --output-volume lower
# bindd = , XF86AudioRaiseVolume, Increase volume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%
# bindd = , XF86AudioLowerVolume, Decrease volume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%

# toggle mute output sound
bindd = , XF86AudioMute, Toggle mute, exec, swayosd-client --output-volume mute-toggle
# bindd = , XF86AudioMute, Toggle mute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle

# toggle mute input / mic  sound
# mic mute toggle with osd. with keyboard that does not have mic mute
# bind = $mainMod ALT SHIFT, M, exec, swayosd-client --input-volume mute-toggle # FIX: swayosd has bug with mic mute
bind = $mainMod ALT SHIFT, M, exec, ~/.config/hypr/scripts/toggle-mic.sh
# bind = $mainMod ALT SHIFT, M, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle

# Capslock (If you don't want to use the backend) bindsym --release Caps_Lock exec swayosd-client --caps-lock 
# Capslock but specific LED name (/sys/class/leds/) bindsym --release Caps_Lock exec swayosd-client --caps-lock-led input19::capslock
# bindel = ,Caps_Lock, exec, swayosd-client --caps-lock # FIX: . always shows on


bindd = , XF86AudioPlay, Play/pause, exec, playerctl play-pause
bindd = , XF86AudioNext, Next track, exec, playerctl next
bindd = , XF86AudioPrev, Previous track, exec, playerctl previous

# mic mute toggle with osd
# bindel = ,XF86AudioMicMute, exec, swayosd-client --input-volume mute-toggle

bindd = , XF86Lock, Lock screen, exec, hyprlock

# other version
# Laptop multimedia keys for volume and LCD brightness
# bindel = ,F7, exec, swayosd-client --output-volume raise
# bindel = ,F6, exec, swayosd-client --output-volume lower
# bindel = ,F4, exec, swayosd-client --output-volume mute-toggle
# bindel = ,F8, exec, swayosd-client --input-volume mute-toggle
# bindel = ,F10, exec, swayosd-client --brightness +10
# bindel = ,F9, exec, exec swayosd-client --brightness -10

